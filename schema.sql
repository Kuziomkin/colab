


CREATE TABLE fbi_wanted(title VARCHAR, description VARCHAR, subject VARCHAR, status VARCHAR, caution VARCHAR, aliaces VARCHAR, reward_text VARCHAR, "publication" DATE, sex VARCHAR, race VARCHAR, hair VARCHAR, height_max DOUBLE, nationality VARCHAR, age_range VARCHAR, weight VARCHAR, dates_of_birth_used VARCHAR, place_of_birth VARCHAR, languages VARCHAR, warning_message VARCHAR, occupations VARCHAR, complexion VARCHAR, reward DOUBLE, age_min BIGINT, age_max BIGINT, subject_group VARCHAR);




